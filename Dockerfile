FROM hub991/nodeargo
#FROM 指令指明当�?:FROM 指令指明当前的镜像基于哪个镜像构建�的镜像基于哪个镜像构�?


EXPOSE 3000
#EXPOSE 指令来暴露应用需要的端口。例如，如果你的应用运行�?3000 端口，你应该添加以下行到你的 Dockerfile�?

#ENV UUID=
#ENV AG_DOMAIN=
#ENV AG_AUTH=
#ENV 指令定义多个环境变量


